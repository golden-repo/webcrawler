= `Hey, ${name}!, wallet transaction request | Status: ${(wallet_request.RequestStatus == 0 || wallet_request.RequestStatus == 2) ? 'PENDING' : (wallet_request.RequestStatus == 1 ? 'APPROVED' : 'REJECT')}`